// JavaScript Document
function setCookie(name, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = 'ok=' + nameofitem + keycookie; 
}

function setCookie1(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem1").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie;   
}
function setCookie2(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem2").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie;  
}
function setCookie3(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem3").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie; 
}
function setCookie4(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem4").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie; 
}
function setCookie5(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem5").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie; 
}
function setCookie6(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem6").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie; 
}
function setCookie7(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem7").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie; 
}
function setCookie8(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem8").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie; 
}
function setCookie9(cname, cvalue, exdays) {
	var keycookie = document.cookie;
	var nameofitem = document.getElementById("nameofitem9").innerHTML;
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = nameofitem +', '+ keycookie; 
}

	

